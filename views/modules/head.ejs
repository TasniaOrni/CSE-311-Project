<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Project Name</title>

  <!-- Bootstrap 5 css -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/footer.css">
  <link rel="stylesheet" href="/css/index.css">
</head>

<body class="d-flex flex-column w-100">
  <header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container">
        <a class="navbar-brand" href="/">
          <img src="/placeholder.png" height="34px" alt="project logo">
        </a>
        <button type="button" class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
          <div class="navbar-nav mx-auto d-flex justify-content-evenly">
            <% if (url === '/') { %>
            <a href="/" class="nav-link text-decoration-none mx-2 active">Home</a>
            <% } else { %>
            <a href="/" class="nav-link text-decoration-none mx-2">Home</a>
            <% } %>

            <% if (url === '/jobs') { %>
            <a href="/jobs" class="nav-link text-decoration-none mx-2 active">Jobs</a>

            <% } else { %>
            <a href="/jobs" class="nav-link text-decoration-none mx-2">Jobs</a>

            <% } %>

            <% if (url === '/contact') { %>
            <a href="/contact" class="nav-link text-decoration-none mx-2 active">Contact</a>

            <% } else { %>
            <a href="/contact" class="nav-link text-decoration-none mx-2">Contact</a>
            <% } %>

          </div>
          <div class="mx-2 d-inline-block">
            <% if (!isLogin) { %>
            <a href="/login" class="link-dark text-decoration-none mx-auto">Login</a> <span> / </span>
            <a href="/signup" class="link-dark text-decoration-none mx-auto">Sign up</a>
            <% } else { %>
            <a href="/api/auth/logout" class="link-dark text-decoration-none mx-auto"><i class="fa-solid fa-arrow-right-from-bracket"></i> Logout</a>
            <% } %>
          </div>
        </div>
      </div>
    </nav>
  </header>